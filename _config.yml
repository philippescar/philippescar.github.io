# Site
name: "Thoughts From Phil"
title: "Thoughts From Phil"
description: "A blog about my thoughts regarding the current state of the Tech Industry"
url: https://philippescar.dev
baseurl: "/"
favicon: "assets/images/favicon/favicon.png"
logo: "assets/images/phil-carvalho.png"
permalink: /:year/:month/:day/:title
paginate: 6
timezone: "Europe/Berlin"

# Build Settings
remote_theme: chriskyfung/amp-affiliately-jekyll-theme

# Other
markdown: kramdown
highlighter: rouge

# Plugins
plugins:
  - jekyll-archives
  - jekyll-feed
  - jekyll-github-metadata
  - jekyll-paginate
  - jekyll-redirect-from
  - jekyll-seo-tag
  - jekyll-sitemap

# Archives
jekyll-archives:
  enabled:
    - categories
    - tags
  layout: archive
  permalinks:
    category: "/category/:name/"
    tag: "/tag/:name/"

# Navigation features
nav:
  buttons:
    gh_sponsors: false # Show GitHub Sponsors button
    invitation_codes: false # Disable Invitation Codes
  global:
    menu: true
    dropdown: false # Show menu with dropdown submenu
  local:
    items:
      - name: About Me
        url: about/
      - name: My Experience
        url: https://www.linkedin.com/in/philippescar/
    menu: false
    title:
      limit: 40

# List Sorting
sort_categories: false
sort_tags: false

# Content Post-processing
target_blank: false
anchor_link: true

# Sidebar Features
sidebar:
  featured: false
  recent_posts: true

# Google Custom Search Engine ID
cse_id: b749edd80672d4c97

# Analytics
google_analytics: #'UA-72575541-9' # Google Analytics Tracking ID
ga4: "G-XMHYVLW301" # Google Analytics 4 Measurement ID
gtm: "GTM-KXH3DP4" # Google Tag Manager ID

# Google Ads
adstyle: true
adsense:
  client_id: "ca-pub-2988558136338994" # Google Adsense client ID
  auto_ads: true
  ad_slot:
    # before_content: 2454411055 # Ad Slot that places before content
    sidebar: #1391498756 # Ad Slot that places to sidebar

# Social Buttons
social:
  email: pcarvalho.ti@gmail.com
  github: philippescar
  linkedin: philippescar
  twitter: opsphil

# Buy Me a Coffee
buymeacoffee: philippescar

# Copyright Start Year
copyright:
  prefix: Copyright © 2022-
  ownership: Philippe Carvalho
  link: https://philippescar.dev/

############################################################
# Site configuration for the WebP Generator Plugin
# The values here represent the defaults if nothing is set
webp:
  enabled: true

  # The quality of the webp conversion 0 to 100 (where 100 is least lossy)
  quality: 75

  # List of directories containing images to optimize, nested directories will only be checked if `nested` is true
  # By default the generator will search for a folder called `/img` under the site root and process all jpg, png and tiff image files found there.
  img_dir: ["/images/", "/assets/images", "/assets/images/posts"]

  # Whether to search in nested directories or not
  nested: true

  # add ".gif" to the format list to generate webp for animated gifs as well
  formats: [".jpeg", ".jpg", ".png", ".tiff"]

  # File extensions for animated gif files
  gifs: [".gif"]

  # Set to true to always regenerate existing webp files
  regenerate: false

  # Local path to the WebP utilities to use (relative or absolute)
  # Omit or leave as nil to use the utilities shipped with the gem, override only to use your local install
  webp_path: nil

  # List of files or directories to excludes
  # e.g. custom or hand generated webp conversion files
  exclude: []

  # append '.webp' to filename after original extension rather than replacing it.
  # Default transforms `image.png` to `image.webp`, while changing to true transforms `image.png` to `image.png.webp`
  append_ext: false
############################################################
